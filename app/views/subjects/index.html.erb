<div class = "container">
  <h1> Fachgebiete </h1>
  <table class = "table table-hover">
    <thead class = "threa-light">
      <tr>
        <th>
          Name
        </th>
      </tr>
    </thead>
    <tbody> <% @subjects.each do |s| %>
        <tr>
          <td><%= link_to s.name, show_subject_path(s.id) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
