<div class = "container">
  <h1> Bücher im Fachgebiet <%= @subject.name %> </h1>
  <table class = "table table-hover">
    <thead class = "threa-light">
      <tr>
        <th>
          Name
        </th>
      </tr>
    </thead>
    <tbody> <% @subject.books.each do |b| %>
        <tr>
          <td><%= link_to b.title, show_book_path(b.id) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to 'Fachegebiete', subjects_path%>
</div>
